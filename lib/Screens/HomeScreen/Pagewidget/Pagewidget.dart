import 'package:flutter/cupertino.dart';

class Pagewidget extends StatelessWidget {
  const Pagewidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Pagewidget");
  }
}
